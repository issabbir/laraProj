Create env file first, and make changes in db connetion.

1) Delete composer.lock + vendor
2) remove ext* from composer json
3) composer install
4) composer require laravel/ui
5) php artisan ui vue --auth
6) npm install
7) npm run watch


>>> php artisan migrate:refresh (to migrate new table column)


>>> composer create-project --prefer-dist laravel/laravel laraproj (install laravel 8)
>>> composer require laravel/jetstream
>>> php artisan jetstream:install livewire
